bundle:
  name: diabetic-outcomes

include:
  - resources/*.yml

variables:
  catalog:
    default: sandbox_us_west_2
  schema:
    default: hls_demo_omop_analytics

targets:
  dev:
    default: true
    mode: development
